# Foodgram — Кулинарное сообщество

### Foodgram — это современная онлайн-платформа для любителей кулинарии, где можно делиться рецептами, находить вдохновение для приготовления новых блюд и планировать покупки с помощью умного списка.

## ✨ Возможности
### 👨‍🍳 Для кулинаров
* Публикация рецептов: Создавайте подробные рецепты с фотографиями, ингредиентами и пошаговыми инструкциями.

* Категории и фильтрация: Сортируйте рецепты по категориям (Завтрак, Обед, Ужин) и времени приготовления.

* Избранное: Сохраняйте понравившиеся рецепты других пользователей в свою личную коллекцию.

* Подписки: Следите за обновлениями любимых авторов в персональной ленте.

### 🛒 Организация покупок
* «Список покупок»: Автоматически формируйте список необходимых продуктов для выбранных рецептов.

* Экспорт списка: Скачивайте список в удобном формате для похода в магазин.

### 🌐 Социальный функционал
* Публичный профиль: Делитесь своими кулинарными достижениями.

* Лента активности: Следите за новыми рецептами от авторов, на которых подписаны.

### 🚀 Быстрый старт
#### Онлайн-доступ
Готовое приложение доступно по адресу: https://foodgram.ppavel.pro

Для начала работы:

1. Перейдите на сайт.

2. Зарегистрируйте новый аккаунт или войдите в существующий.

3. Начните исследовать рецепты или опубликуйте свой первый!

### Локальная разработка (из репозитория)
Проект использует микросервисную архитектуру с контейнеризацией Docker. Для запуска полного стека (бэкенд, фронтенд, базы данных) необходимы Docker и Docker Compose.

Клонирование и настройка
```
git clone https://github.com/Pave138/foodgram.git
cd foodgram
```
Создайте и настройте файл .env на основе примера .env.example в корне проекта. Обязательно укажите секретные ключи, настройки баз данных и другие переменные окружения.

Запуск в среде разработки
```
docker-compose up
```
После сборки и запуска контейнеров приложение будет доступно:

* Frontend (React): http://localhost/

* Backend API (Django REST): http://localhost/api/

* Админ-панель Django: http://localhost/admin/

Запуск в production-режиме
Для деплоя используйте отдельный файл конфигурации:

```
docker-compose -f docker-compose.production.yml up -d
```
Эта конфигурация оптимизирована для работы в продакшене (например, настройки Nginx, статические файлы).

### 📁 Структура проекта
Основные директории в репозитории:

* backend/ — исходный код Django-приложения (API, модели, админка).

* frontend/ — исходный код React-приложения.

* nginx/ — конфигурация веб-сервера.

* data/ — вероятно, содержит фикстуры или SQL-дамps для начального заполнения БД.

* postman_collection/ — коллекция запросов для тестирования API.

* .github/workflows/ — конфигурации для CI/CD (GitHub Actions).

* docker-compose.yml — конфигурация для разработки.

* docker-compose.production.yml — конфигурация для production-развёртывания.

### 📚 Документация API
Для ознакомления с полным перечнем эндпоинтов и работой с API вы можете импортировать коллекцию Postman из папки postman_collection/. API предоставляет полный набор операций CRUD для управления рецептами, ингредиентами, подписками и списком покупок.
